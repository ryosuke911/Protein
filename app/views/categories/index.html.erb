<h1>カテゴリ編集</h1>
    <table class="table">
        <% @categories.each do |category| %>
            <tr>
            <% if category.account == current_account %>
                <td><%= link_to category.name,controller: 'posts', action: 'select_category_index', id: category.id %></td>
                <td><%= link_to("編集","/categories/edit/#{category.id}") %></td>
            <% end %>
            </tr>
        <% end %>
	</table>
</div>