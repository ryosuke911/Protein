<h1 class="ml-4">カテゴリ一覧</h1>
<hr>

<div class="ml-4">
    <% @categories.each do |category| %>
        <p><%= link_to category.name,controller: 'posts', action: 'select_category_index', id: category.id %></p>
    <% end %>
</div>